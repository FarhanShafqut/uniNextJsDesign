import React from 'react'

export default function SubjectButton(props) {
  return (
                 <div>
                    <span>{props.title}</span>
                    <span>{props.txt}</span>
                </div>
  )
}
